/**
 * Video tiles per page in the meeting UI (layout constraint).
 * Max people per meeting comes from the API (`maxParticipants`); server allows up to 100 via env.
 */
export const MEETING_VIDEO_PAGE_SIZE = 6;
